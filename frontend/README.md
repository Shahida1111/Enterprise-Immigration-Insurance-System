# JKHimmigration

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 16.2.14.

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The application will automatically reload if you change any of the source files.

# Enterprise Immigration Insurance System

A professional management system for immigration insurance workflows.

## 🛠 Setup Instructions
1. **Clone the project**
2. **Install Dependencies:** 
   Run `npm install` (Note: `node_modules` are excluded from Git for efficiency).
3. **Start the App:** 
   Run `ng serve` and open `http://localhost:4200/`.

## 📂 Project Status
* **Version:** 1.0.0 (First Commit)
* **Git Optimized:** `.gitignore` is active to exclude build artifacts and large ZIP files.
