import { CapitalizeInputOnlyDirective } from './capitalize-input-only.directive';

describe('CapitalizeInputOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new CapitalizeInputOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
